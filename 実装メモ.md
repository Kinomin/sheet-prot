# 席メイト — 実装メモ（β版）

指示書に対する実装状況と、その後の修正・構成変更の記録。
アプリ名は「自動座席表作成ツール」として開発を始め、第18節で「席メイト」に変更しました。
第17節までの本文は当時の名称のまま残しています。

> **現在の構成（第14〜16節が最新）**
> 画面はGitHub Pages上の単一HTMLファイル。名簿は利用者本人のGoogleドライブ（アプリ専用領域）、
> アカウント・会員種別・印刷回数はSupabase、決済はStripe（Edge Functions経由）、
> 問い合わせはGoogle Apps Script経由でスプレッドシート記録＋Gmail通知。
> 第1〜13節は、そこへ至るまでの実装と修正の経緯です。

---

## 1. 今回作ったもの

`index.html`（旧: 座席表ツール_プロトタイプ.html） — ビルド不要の単一ファイル。ブラウザで開けばそのまま動きます。
GitHub Pagesで公開した際にURLの日本語が文字化けしないよう、ファイル名を`index.html`に変更しました。
※ 見本データは第16節で廃止し、現在は白紙から始まります（以下は当時の記述）。

動作確認の目安ルート（3クリック以内で座席表が完成することの検証）:

1. ホーム →「1年1組」
2. 「座席表履歴」タブ →「新しい座席表を作成」→ 開始日を確認して作成
3. ツールバーの「ランダム配置」→ 条件を選んで実行 → 「印刷 / PDF」

---

## 2. 指示書に対する実装状況

| 要件 | 状態 | 補足 |
|---|---|---|
| ホーム／テンプレート詳細／座席表編集の3画面 | ○ | |
| テンプレート単位の保存項目一式 | ○ | 表示ルール（担任＝クラス名＋担任名／選択＝授業名＋担当名）も反映 |
| 履歴管理（開始日必須・複製・名称変更・削除） | ○ | |
| 名簿CSV読み込み・必須4列の検証 | ○ | .xlsx にも対応（下記5・外部ライブラリ不使用） |
| ふりがな→ローマ字自動生成 | ○ | 拗音・促音・長音・カタカナ入力に対応 |
| 用紙設定（A4/B5 × 縦横）と自動調整 | ○ | 座席サイズ・余白・フォントをJSで再計算し必ず1ページに収める |
| 教室レイアウト（教卓・入口・出口・黒板） | ○ | ドラッグ移動／削除、印刷にも反映 |
| 基本グリッド生成 → 1席単位の編集 | ○ | 削除席はグレーで位置維持・詰めない・印刷では非表示 |
| 列単位の座席追加（前／後／任意位置） | ○ | 「＋前」「＋後」ボタン＋グレー席クリックで復元 |
| 座席を独立オブジェクトで管理 | ○ | `{id, col, row, enabled}` ＋ 固定フラグ・生徒は別レイヤで保持 |
| 名前カード（検索・未配置のみ・ドラッグ） | ○ | |
| 配置済み座席へのドロップ時の確認ダイアログ | ○ | 「はい」で入れ替え、元の生徒はカード一覧へ戻る |
| 交換モード（クリック2回） | ○ | ショートカット `S` |
| Undo / Redo（Ctrl+Z / Ctrl+Y、50操作以上） | ○ | 60操作分保持 |
| ランダム配置の事前確認と条件チェック | ○ | 固定内容を文章で提示 |
| 条件の優先順位と「部分採用しない」 | ○ | 下記3 |
| 配置失敗時のエラー表示 | ○ | 原因を列挙して中止 |
| 印刷（右上に開始日・クラス名・担任名） | ○ | `@page` を設定に応じて動的生成 |
| 座席表はスクロール禁止・常に全体表示 | ○ | 名前カード一覧のみスクロール |
| ローカル完結（外部送信なし） | ○ | 通信コードなし |
| IndexedDB永続化 | ○ | ブラウザを閉じても保持。書き出し／読み込み（JSON）も追加（下記5） |

---

## 3. ランダム配置アルゴリズム

指示書の優先順位を**絶対条件**として実装。上位条件を破って下位を満たすことはありません。

```
① 固定席 → ② 固定生徒 を先に確定
③ NGペア（前後左右の隣接禁止・斜めは対象外）
④ 前回と同じ席を禁止
⑤ 男女交互（左右で隣り合う席の性別が異なる）
⑥ 残りをランダム
```

行優先順でのバックトラッキング探索。各席を埋める時点で「確定済みの左・上の隣席」だけを見て枝刈りします。
解が存在しない場合は**部分的な配置を一切採用せず**、
原因候補（NGペア過多／固定席の競合／男女比の不成立など）を挙げて中止します。

> 上位条件が優先されるため、**①で固定した席には④を適用しません**。
> 毎回同じ席に固定している生徒（視力配慮など）がいるときに、
> ④を選んだだけで全体が失敗してしまうのを避けるためです（第23節で修正）。

探索の詳細は第23節を参照してください（同性の生徒をまとめて扱う枝刈りと、打ち切り時のやり直し）。

Nodeでの検証済みケース: 固定席＋NGペアあり／男女交互（男18・女17）／前回と同じ席0件／
解が存在しない条件で正しくエラー。第24節でさらに10通りの構成（1席のみ／1列のみ／歯抜けの座席／
全員固定／NGペア10組／8×8の60名／全条件同時など）で配置結果を検算しています。

---

## 4. 本番（Next.js）への移行

プロトタイプの構造はそのまま本番の責務分割に対応させてあります。

```
app/
  page.tsx                        → viewHome
  templates/[id]/page.tsx         → viewTemplate（タブ5枚）
  templates/[id]/charts/[cid]/page.tsx → viewEditor
components/
  seat/SeatGrid.tsx  Seat.tsx  RoomItem.tsx  NameCardList.tsx
  layout/LayoutEditor.tsx        → paneLayout
lib/
  romaji.ts                      → toRomaji
  solver.ts                      → solve（純関数・そのまま移植可）
  fit.ts                         → fitSheet（用紙フィット計算）
  db.ts                          → idb-keyval / Dexie
stores/
  useTemplateStore.ts (Zustand)  → store
  useHistoryStore.ts  (Zustand)  → pushUndo / doUndo / doRedo
```

- **`store`** が唯一の永続化窓口です。`get / add / save / remove` の4メソッドを
  IndexedDB（Dexie もしくは idb-keyval）に置き換えるだけで、他のコードは変更不要です。
- **`solve()` と `toRomaji()` は副作用のない純関数**なので、TypeScript化してそのまま移植し、
  Vitest でテストを固定してください（本メモの検証ケースをそのまま流用できます）。
- ドラッグ＆ドロップは HTML5 DnD で組んでいますが、本番はタブレット対応のため **dnd-kit** に置き換えます
  （`PointerSensor` + `TouchSensor`）。ドロップ判定の意味論（空席へ配置／占有席は確認ダイアログ／席から席は入れ替え）は
  `placeStudent()` にまとまっているので、そのままロジックを移せます。
- 用紙フィットは `fitSheet()` に集約。CSSだけで組むと印刷時にズレるため、
  画面用と印刷用（mm→px、`@page` マージン差し引き）で同じ関数を切り替える方式にしています。

---

## 5. 残タスク（すべて対応済み）

1. **.xlsx 読み込み** — SheetJSは使わず、ブラウザ内蔵APIのみで実装（`DecompressionStream('deflate-raw')` でZIP展開 → `DOMParser` で
   `workbook.xml` → `_rels` → `worksheets/sheet1.xml` / `sharedStrings.xml` を読む自前パーサ）。外部通信・外部ライブラリなしの方針を維持しています。
   非対応ブラウザ（DecompressionStream未実装）の場合はCSVへの書き出しを案内するエラーダイアログを出します。
   CSVパーサも引用符・カンマ・改行入りセル、二重引用符エスケープに対応させました（従来の単純splitから置き換え）。
2. **IndexedDB永続化** — `store` の同期API（get/add/save/remove）はそのまま維持し、内部でIndexedDBへの書き込みスルーを実装。
   起動時に `store.load()` でIndexedDBから読み込み、空なら見本データを生成して保存します。
   ホーム画面に全データのJSON書き出し／読み込み（読み込みは確認ダイアログ付きの全置換）も追加しました。
3. **ローマ字の手修正** — 「1名ずつ追加」のローマ字欄のreadonlyを外し、ふりがな入力時は自動生成、
   ローマ字欄を手で編集した後は自動上書きしない方式に。名簿テーブルのローマ字セルも直接編集可能にしました。
   CSV/xlsxに任意で「ローマ字」列がある場合はそちらを自動生成より優先します（助詞読みの例外などを教員が指定可能）。
4. **タッチ操作の詰め** — 交換モード中は座席の見た目のサイズを変えずに、クリック判定領域のみ44×44px以上を確保。
   あわせて、実際には適用されていなかった `.mode-edit` というデッドなCSSセレクタ（実際のクラス名は
   `mode-place`/`mode-swap`/`mode-room`）を `mode-place`/`mode-swap` に修正しました。
5. **将来拡張の受け皿** — 座席オブジェクトに `tags:[]` を追加済み（現時点では未使用の受け皿）。
   `solve()` 内の制約判定を `SEAT_CONSTRAINTS` / `PAIR_CONSTRAINTS` という優先順位つきの述語リストに整理し、
   視力配慮・支援配慮・仲良しペアなどを追加する際は述語を1つ足すだけで済む構造にしました。

---

## 6. 判断が必要な点

- **男女交互の定義**：左右の隣接のみ交互（＝縦の列は同性が並びうる）としました。
  市松模様（前後も交互）にすると6×6・男女比が偏るクラスではほぼ解が出ないためです。→ご確認いただき、現状のまま（左右のみ交互）で確定しました。
- **レイアウト変更と履歴の関係**：座席表を作成した時点のレイアウト・固定席・NGペアを
  各履歴にスナップショットとして保存しています。これにより「テンプレートを直しても過去の座席表は変わらない」を保証しています。
- **削除席の扱い**：画面ではグレー表示で位置を維持、印刷時のみ非表示という指示どおりですが、
  周囲に空白が残ります。印刷時に全体を中央寄せするか、位置を保ったままにするかはご指示ください（現在は位置維持）。

---

## 7. 追加実装（生徒側／教員側の切替 ほか）

- **生徒側／教員側の自動切替**：座席データは常に生徒側（生徒から見た配置）基準の1系統のみを保持し、
  教員側（教卓から見た配置）は表示・印刷のときにその場で自動生成します。二重管理はしていません。
  編集ツールバーに「生徒側／教員側」トグルを追加（用紙サイズ・向きと同じ操作感）。
  教員側＝教卓から見た図は部屋全体を180度回転させた見え方（下から順に黒板→教卓→1列目…となる）のため、
  座席は `gridColumn`／`gridRow` を `cols-col` ／ `rows-row` で計算し行・列とも反転、教室備品（黒板・教卓・入口・出口）は
  `x`・`y`座標をどちらも `100-値-幅(高さ)` で反転しています（詳細は下記8）。教室レイアウト編集のドラッグ操作も
  教員側表示中は自動的に座標変換して破綻しないようにしています。
- **編集中のスクロール位置維持**：`render()` が呼ばれるたびに `#app` を丸ごと作り直すため、座席レイアウトの
  1マス編集や保存のたびにタブ先頭までスクロールが戻っていた問題を修正。直前と同じ画面／タブへの再描画時のみ
  スクロール位置を維持し、実際に別の画面・タブへ移動したときは今まで通り先頭から表示します。
- **印刷時に編集用の表示を消去**：「固定」バッジや固定席の縁取り、交換モードの選択枠・ドロップ枠などは
  編集中にだけ意味がある情報のため、`@media print` で非表示にしました。印刷される座席表には生徒名・番号・
  ローマ字・教室備品のみが出力されます。

---

## 8. 追加修正（ご指摘反映）

- **座席固定の選び方をビジュアル化**：「固定席・NGペア」タブの座席選択を、テキストのプルダウンから
  座席レイアウトを模したグリッド（`座席レイアウト`タブと同じ`前から◯列目・△番`の並び）に変更。
  クリックした席がオレンジ色でハイライトされ、既に他の生徒で固定済みの席は赤色＋生徒名で表示されるため、
  一覧性・誤操作の防止ともに改善しています。
- **教員側ビューを180度回転に修正**：教員側（教卓から見た図）は「左右反転のみ」ではなく、
  「下から順に黒板→教卓→1列目→…」となる180度回転が正しいとのご指摘を反映し、行・列とも反転するよう修正しました
  （旧実装は`.seatgrid`に`direction:rtl`を当てる左右反転のみで、行の前後関係は反転していませんでした）。
  また「生徒側（生徒から見た配置）」「教員側（教卓から見た配置）」という説明文言は印刷物に不要とのことで削除し、
  トグルボタンの「生徒側」「教員側」ラベルのみを残しています。
- **印刷を「画面中央でのプレビュー確認」経由に変更**：以前は印刷ボタンを押すと即座に印刷用サイズへ
  リサイズされ、編集画面の小さな枠内で見た目が崩れて確認しづらい状態のまま`window.print()`が呼ばれていました。
  今回、印刷ボタン（およびCtrl+P）を押すと、実物の`.sheet`要素を画面中央のオーバーレイへ一時的に移動して
  大きく表示するプレビュー確認ダイアログを挟むようにし、「はい、印刷する」を押したときだけ実際の印刷を実行します。
  プレビュー中は固定バッジ・固定席の縁取りなどの編集専用の表示も非表示にし（`.print-preview`スコープのCSSで
  `@media print`と同じ抑制を再現）、座席・教室備品はクリック等ができないようにしています。
  「キャンセル」・背景クリック・Escキーのいずれでも、実要素を元の位置へ戻して編集画面へ復帰します。

---

## 9. 追加修正（第2弾）

- **教員側で黒板・教卓が座席と重なる不具合を修正**：座席グリッドの表示領域（`.seatgrid`）が常に
  「部屋の下80%」固定だったため、教員側（黒板・教卓が反転して下側に来る表示）にしても座席側の表示領域が
  変わらず、黒板・教卓と座席の最後列が重なっていました。教員側のときは座席グリッドの表示領域を
  「部屋の上80%」に切り替えるよう`fitSheet()`を修正し、重なりを解消しました。
- **テンプレートの削除機能を追加**：「基本設定」タブの末尾に確認ダイアログ付きの「このテンプレートを削除する」
  ボタンを追加しました。名簿・座席レイアウト・座席表履歴を含めすべて削除され、元に戻せない旨を明記しています。
- **ホームのJSON書き出し／読み込みを削除**：前回追加した全データのエクスポート／インポート機能は
  不要とのことで、ボタンごと削除しました（IndexedDBへの永続化そのものは維持）。
- **種別表記をHR／授業に変更**：テンプレート種別の表示・選択に使っていた「担任クラス」「選択授業」の文言を、
  それぞれ「HR」「授業」に変更しました（新規作成ダイアログの切替ボタン、テンプレート一覧のチップ、
  基本設定タブの表示ルール説明など）。

---

## 10. 追加修正（第3弾・不具合とスマホ対応）

- **座席固定で席を選べない不具合**：座席ピッカーのグリッドを `<label>` で包んでいたことが原因でした。
  HTMLの仕様上、`<button>` はラベル対象（labelable）要素のため、どの席をクリックしてもラベルが
  「最初のボタン」へクリックを転送してしまい、選択が常に1席目に戻っていました。`<div>` に変更して解消しています。
- **出口が動かせない不具合**：座席グリッド（`.seatgrid`）が `inset:0` で部屋全体を覆っており、
  その下にある備品のクリックを奪っていました。入口は座席グリッドの外（上部）にあるため動かせていた一方、
  下部にある出口だけが掴めない状態でした。教室レイアウト編集中はグリッド側の当たり判定を無効化（`pointer-events:none`）し、
  備品を前面（`z-index`）に出すことで解消しています。
- **備品が座席と重なったときの自動退避**：指を離した時点で備品が座席の並びと重なっていた場合、
  実際に並んでいる座席の外接矩形を計測し、いちばん近い外側（上下左右のうち移動量が最小の方向）へ
  自動的に押し出すようにしました（`seatBoundsPct()` / `pushOutOfSeats()`）。
- **座席表履歴がスマホで崩れる問題**：1行を「情報」と「操作ボタン」の2ブロックに整理し、
  日付・名称を横並び、配置数・終了日・更新日時をその下の小さな行にまとめました。
  スマホ幅では情報とボタンが上下に分かれ、ボタンは幅いっぱいに均等配置されるため、枠外へのはみ出しと
  縦長化が解消されます。
- **スマホでの座席表編集画面**：スマホ幅では名前カードのパネルを右横ではなく下段に移し、
  カードは「番号＋苗字」だけの省スペース表示（フルネームはツールチップで確認可）に切り替えます。
  さらにツールバーは折り返すと5段に達して座席表を圧迫していたため、1段の横スクロールに変更しました。
  これにより375px幅の端末で、ツールバーの高さが約290px→51px、座席表の幅が約140px→359pxに広がります。

---

## 11. 追加修正（第4弾・入口／出口の既定位置）

- **入口・出口を右側の既定に変更**：入口は左上（x:2, y:2）に置いていましたが、入口・出口ともに右側へ寄せ、
  入口を右前方（x:84, y:10）、出口を右後方（x:84, y:91）に変更しました。
- **縦向きでの重なりを事前に回避**：座席の占有範囲を実測したところ、A4横では座席が左右20%ずつを空ける一方、
  **A4縦では座席が横幅いっぱい（0〜100%）まで広がり、空くのは上下の帯だけ**になることが分かりました。
  そのため旧既定の出口（y:88）は縦向きにすると座席へ食い込んでいました。新しい既定値は縦・横どちらでも
  座席の外側に収まる位置（入口 y:10 は上の帯、出口 y:91 は下の帯）にしてあります。
- **レイアウト確定時の自動回避**：既定値だけでなく、用紙の向きや座席数の変更で占有範囲が変わった場合にも
  対応できるよう、レイアウトが確定するたび（`fitSheet()` の最後）に備品が座席と重なっていないか判定し、
  重なっていれば最寄りの外側へ表示位置を逃がす `avoidRoomOverlaps()` を追加しました。
  こちらは**表示位置だけを調整し保存データは書き換えない**方式のため、向きを戻せば元の配置に復帰します。
  ドラッグ時の掴み位置も、この表示補正を考慮して実際の描画位置から取るよう修正しています。

---

## 12. 追加実装（第5弾・固定席の同期修正とログイン／課金）

- **固定席が座席ピッカーに反映されない不具合を修正**：固定席は「テンプレートの既定（`t.fixedSeats`）」と
  「その座席表のスナップショット（`c.fixedSeats`）」の2か所で保持していますが、編集画面の
  「この席を固定する」は`c.fixedSeats`だけを更新し、「固定席・NGペア」タブの座席ピッカーは
  `t.fixedSeats`を読んでいたため食い違っていました。両方へ同期する`setSeatFixed()`を追加し、
  同じ席・同じ生徒の重複登録も同時に防いでいます。
- **ログイン画面**：起動時に会員種別（無料／プレミアム）を選び、Googleアカウントでログインする画面を追加。
  Google Identity Services を必要になった時だけ動的に読み込みます。法人契約の案内も設置しました。
  `APP_CONFIG.googleClientId` が未設定の場合は「お試しログイン」で全機能を確認できます。
- **プレミアム会員（500円/年）の決済**：静的サイトから安全に扱える **Stripe Payment Link 方式**を採用。
  `client_reference_id`（アカウントのsub）と`prefilled_email`を付与して遷移するため、
  将来サーバーを用意したときにwebhookで会員と突き合わせられます。
  決済完了後の戻り先に`?checkout=success`を付けておくと、復帰時に会員種別へ反映します。
- **設定メニュー**：ログイン後の画面右上に設定アイコン（⚙）を設置し、アカウント設定
  （プレミアム会員への変更／無料会員へ戻す／表示名の変更）・問い合わせ・ログアウトを可能にしました。

### 会員種別の判定に関する制約（重要）

サーバーを持たない構成のため、会員種別は端末のlocalStorageに保存しています。
**ブラウザ内の値は利用者自身が書き換えられるため、これだけでは課金の有無を根拠に機能制限をかけることはできません。**
課金と機能を確実に結びつけるには、Stripeのwebhookを受け取って会員状態を保存・照会するサーバーが別途必要です。
差し替え地点として `AUTH.setPlan()` と `AUTH.refreshPlanFromServer()` を用意しています。
また、Stripeのシークレットキー（`sk_...`）は決してこのファイルへ書かないでください。

現時点ではプレミアム限定機能を設けていないため、この制約が実害になる場面はまだありません
（有料機能を追加する段階でサーバー側の検証が必須になります）。

---

## 13. ディープレビューと修正（β版化）

コード全体を精読し、実際にブラウザで再現確認した上で7件を修正しました。
すべて「実際に使うと困る」ことを実証してから直しています。

### 重大（個人情報・印刷物の正しさに関わる）

1. **アカウント間でデータが共有されていた（個人情報漏洩）**
   IndexedDBがアカウント横断で1つだったため、**別のGoogleアカウントでログインすると
   前の先生の生徒名簿がそのまま見える**状態でした（実測：先生Bのログイン後に
   先生Aの生徒名「上原 咲希」等が閲覧可能）。職員室の共用パソコンでは現実に起こります。
   テンプレートに `ownerId` を持たせ、読み込み時に自分のものだけを取り出すよう変更。
   ログイン・ログアウトのたびに `reloadForAccount()` でメモリ上のデータも入れ替えます。
   `ownerId` を持たない旧データは、その端末の利用者のものとして引き継ぎます。

2. **`todayISO()` がUTC基準で日付がずれていた**
   `toISOString()` を使っていたため、**日本時間の0:00〜9:00は前日の日付**になりました
   （実測：JST 7/30 8:00 → "2026-07-29"）。座席表の開始日として印刷される値なので、
   朝の準備中に作ると1日ずれた座席表が刷られます。ローカル日付で組み立てるよう修正。

### 操作を妨げるもの

3. **ラベル文字のクリックで用紙設定・種別が勝手に変わっていた**
   `<label>` でボタン群（`.seg`）を包んでいたため、ラベル文字をクリックすると
   先頭のボタンへクリックが転送されていました（実測：B5 → A4 に無断で変更）。
   `<div>` へ変更。同種の問題は座席ピッカーで既に直していましたが、
   用紙サイズ・用紙の向き・テンプレート種別の3か所が残っていました。

4. **Undoがテンプレート側の固定席を戻していなかった**
   固定席の同期修正（第12節）で `t.fixedSeats` も更新するようにしたのに、
   undoのスナップショットは `c.fixedSeats` だけだったため、固定操作をundoすると
   食い違いが復活していました。スナップショットに `tplFixed` を追加。

5. **名簿を入れ替えると配置が壊れるのに警告がなかった**
   名簿を差し替えると既存座席表の配置が「存在しない生徒番号」を指したまま残り、
   **「配置35席」と表示されるのに実際は誰も表示されない**状態になりました。
   影響件数を数えて確認をとる（`rosterSwapImpact()`）方式にし、
   実行後は存在しない参照を掃除（`purgeMissingStudents()`）します。生徒1名の削除でも同様に掃除します。

6. **全テンプレートを削除しても見本データが復活していた**
   `store.load()` が「空なら見本を投入」だったため、自分で全部消しても再読込で戻りました。
   アカウント側に `seeded` の印を持たせ、初回ログイン時のみ投入するよう変更。

### 誤解を招く表示

7. **「無料会員に戻す」がStripeの自動更新を止めない**
   表示だけ切り替わって課金が続く誤解を招くため、解約はStripeの領収書メールまたは
   問い合わせから手続きする旨を明記。ローカル切替は決済リンク未設定時（動作確認中）のみ表示します。

あわせて、座席表履歴と「前回と同じ席を禁止」の判定を**開始日順**に統一
（`chartsByDate()` / `prevChartOf()`）、終了日 < 開始日の検証、デッドコード除去、
表記を「プロトタイプ」から「β版」に変更しました。

### 回帰確認

ログイン → 見本データ読込 → ランダム配置 → 編集画面 → 印刷プレビューまで通しで確認し、
配置結果を検算しています（男女交互の違反0件／NGペア違反0件／前回と同じ席0件／固定席は全て遵守）。

---

## 14. 一般公開に向けた構成変更（Supabase + Stripe + Googleドライブ）

「同じGoogleアカウントなら端末をまたいでデータを引き継ぐ」「課金はStripe」「サーバー側で実施」
というご要望に沿って、静的サイト単体からクラウド連携構成へ移行しました。

### 全体像

```
ブラウザ（GitHub Pages）
  ├─ Google Drive appDataFolder … 名簿・座席表（利用者本人のアカウント内）
  └─ Supabase
       ├─ Auth（Googleログイン）
       ├─ profiles（会員種別）
       └─ Edge Functions（Stripeの申し込み・解約・webhook）
```

### なぜ名簿をGoogleドライブに置いたか

生徒の氏名・ふりがな・性別は未成年の個人情報です。これを開発者側のデータベースに置くと、
安全管理措置・学校との規約・漏えい時の責任が発生します。
Googleドライブの **appDataFolder**（アプリ専用の隠し領域）に置けば、

- サーバーを持たずにアカウント単位の保存ができる
- データは利用者本人のGoogleアカウントに属し、運営側からは参照できない
- 許可の範囲がこの専用領域に限られ、ドライブ内の他のファイルには一切触れない

という形になり、預かる個人情報を最小化できます。運営側のデータベースには
アカウント情報と会員種別しか保存していません。

### 同期のしくみ

テンプレート1件につき `tpl_<id>.json` を1ファイル、加えて削除記録 `_deleted.json` を持ちます。
同期時は両者を突き合わせ、

- 削除記録が更新時刻より新しければ削除を優先（他端末にも削除が伝わる）
- そうでなければ `updatedAt` が新しい側を採用

というルールで統合します（`mergeTemplates()` は副作用のない純関数）。
保存のたびに2.5秒のまとめ待ちを挟んで送信し、設定メニューから手動同期もできます。

**検証**：マージ規則を11ケースで確認（取り込み／アップロード／新旧の優先／削除の伝播／
削除後に他端末で編集された場合の復活）。さらにDrive APIをメモリ上の偽サーバーに差し替え、
「端末Aで作成→端末Bで復元→端末Bで削除→端末Aでも復活しない」という往復を確認しました。

### 課金（Stripe）をサーバー側で行う理由と構成

以前はPayment Link方式で、会員種別をブラウザのlocalStorageに置いていました。
これは利用者が自分で書き換えられるため、有料機能の根拠にはできません。
今回は次のように、**書き換えられる経路そのものを塞ぎました**。

- `profiles.plan` の更新権限をクライアントから剥奪（RLSに加えて**列レベル権限**を使用）。
  RLSは行単位までしか制御できないため、`grant update (display_name)` で更新可能な列を限定している
- `plan` を書き換えられるのは、Stripeの署名を検証した `stripe-webhook` 関数のみ
- 金額・商品はサーバー側の `STRIPE_PRICE_ID` を必ず使い、クライアントから指定させない
- 解約はStripeカスタマーポータルへ誘導（`create-portal-session`）。
  これにより「画面表示だけ変わって課金が続く」という以前の問題も解消

シークレット（`sk_...`、`service_role`、`whsec_...`）はすべてSupabaseのシークレットに置き、
リポジトリには一切含めていません。`index.html` に置くのは公開前提の値のみです。

### 公開に必要な文書

`terms.html`（利用規約）と `privacy.html`（プライバシーポリシー）を用意し、
ログイン画面と申し込みダイアログから参照しています。自動更新・解約・返金の扱い、
名簿の保存場所、ドライブ権限の範囲を明記しました。

### 未検証の範囲（正直な状態）

Supabase・Google・Stripeの実際の通信は、認証情報がないため**この環境では検証できていません**。
検証済みなのは、同期のマージ規則・同期の往復（スタブ利用）・画面遷移・座席アルゴリズム・
既存機能の回帰です。実サービスとの疎通は `DEPLOY.md` のチェックリストに沿って、
Stripeのテストモードで確認してください。

---

## 15. 会員種別による機能差と問い合わせ窓口

### 機能差

| | 無料会員 | プレミアム会員 |
|---|---|---|
| 座席表の作成・編集 | 無制限 | 無制限 |
| 印刷 | 3回まで | 無制限 |
| 端末間の引き継ぎ | なし | あり |

- **端末間の引き継ぎ**：`SYNC.available()` にプレミアム判定を追加。無料会員は端末内（IndexedDB）保存のみ。
  設定メニューには代わりに「端末間の引き継ぎを使う」という申し込み導線を出す。
- **印刷回数**：`profiles.print_count` で数え、`consume_print()`（security definer・行ロックあり）で
  原子的に消費する。**ブラウザ側では数えない**——localStorageに持たせると消すだけで回避できるため。
  更新可能な列は表示名のみに絞ってあるので、クライアントから `print_count` は書き換えられない。

  ただし率直に言うと、**Webページである以上ブラウザ自身の印刷機能（Ctrl+P）まで塞ぐことはできません**。
  ここでの制限はツールの印刷ボタン経由の回数に対するもので、技術的な封じ込めではなく
  「使い続けるなら購入してもらう」ための線引きです。この点はREADMEにも明記しています。

  なお通信できないときは印刷を止めない方針にしました（授業前の準備を止めてしまうため）。

### 問い合わせ

アプリ内のフォーム（種別・お名前・メール・内容）から送信すると、
Googleスプレッドシートへ1行追記され、同時にGmailへ通知が届きます。

経路は `ブラウザ → Edge Function(send-inquiry) → Google Apps Script → シート/Gmail`。
Apps ScriptのURLと合言葉をサーバー側のシークレットに置くことで、
ブラウザのコードにURLが現れず、外部から直接叩かれにくくしています。
あわせてログイン必須・60秒に1件（`touch_inquiry()`）の制限をかけています。

Gmailでの通知にしたのは、別途メール配信サービスを契約しなくても
Apps Scriptの `MailApp` が所有者のGmailからそのまま送信できるためです
（個人アカウントの送信上限は1日100通程度）。

**検証**：サーバー側の判定をスタブで再現し、無料会員で4回目の印刷が拒否されること、
プレミアムでは回数が消費されないこと、無料会員では同期が無効なこと、
問い合わせフォームが `send-inquiry` を正しい引数で呼ぶことを確認しました。
実サービスとの疎通は認証情報がないため未検証です（DEPLOY.mdのチェックリスト参照）。

---

## 16. ディープレビュー（第2回）と仕様の整理

### 仕様の変更

- **見本データを廃止し、白紙から開始**。初回ログイン時に5件のサンプルを入れていたのをやめ、
  代わりに「最初のテンプレートを作りましょう」という3ステップの案内を出す。
  これに伴い `seed()` / サンプル名簿 / `AUTH.markSeeded()` を削除（約2700文字分）。
- **お試しログインを廃止し、Google認証に一本化**。
  無料会員・プレミアム会員のどちらで入る場合もGoogleログインを経由する。
  会員種別の2タブとGoogleボタン1つ、という構成に整理した。
  接続先が未設定のときは、動かない状態であることをはっきり表示する。
- **「今後の追加機能」の記載を削除**。追加機能を無料側でも提供する可能性があるため、
  プレミアムの提供内容として約束しない。現在の差は「印刷回数」と「端末間の引き継ぎ」の2点のみ。

### 見つかった不具合と修正

いずれもブラウザ上で再現を確認してから直した。

1. **【重大】ログインから約1時間後、端末間の同期が二度と復帰しない**
   Googleのアクセストークンは約1時間で失効するが、`clearToken()` が
   Supabaseセッション側に残ったトークンを消していなかった。このため401後の再試行でも
   同じ失効トークンを使い、GISによる静かな取り直しに到達しなかった
   （実測：2回とも `EXPIRED_GOOGLE_TOKEN` を送出して失敗）。
   トークンの取得時刻から期限を持たせ、`clearToken()` でセッション側も破棄するよう修正。

2. **ポップアップが塞がれて同期できない場合の出口がなかった**
   自動同期は利用者の操作を伴わないため、トークン再取得のポップアップがブラウザに塞がれる
   （実測：`Failed to open popup window`）。これを失敗ではなく「操作待ち」として扱い、
   「『今すぐ同期』を押してください」と案内。手動実行時は確認画面を開いてよい扱いにして復帰できるようにした。

3. **同意を拒否したときに画面が無反応になる**
   `#error=access_denied` で戻ってきた場合を見ておらず、ログイン画面に戻るだけだった。
   エラーを検知して理由を表示するよう修正。

4. **`/index.html` を直接開いた状態でログインすると失敗する**
   戻り先URLに `index.html` が入り、Supabaseの許可リスト（`.../sheet-prot/`）と一致しないため。
   戻り先を正規化して末尾の `index.html` を落とすようにした。

5. **会員情報が無いと「印刷回数を使い切りました」と誤表示される**
   サインアップ時のトリガーが動かなかった場合に `no_profile` が返り、上限到達と同じ扱いになっていた。
   DB側で行を作ってから続けるようにし、クライアントでも理由を区別して表示を分けた。

6. **削除記録がアカウント間で共有されていた**
   共用パソコンで別の先生の削除記録が自分のドライブへ書き出される状態だったため、
   アカウントごとのキーに分離した。あわせて、同期で取り込んだテンプレートの所有者IDを
   必ず現在のアカウントで上書きするようにした（これを怠ると再読込で消えたように見える）。

---

## 17. ディープレビュー（第3回・決済と問い合わせが動かない件）

「決済ページに移行できない」「問い合わせを送信できない」という報告を受けて、
クライアントからEdge Functions、データベースまでの経路を精読した。
**両方が同時に失敗するのは共通の原因があるはず**という見立てから入り、実際にそうだった。

### 根本原因：CORSヘッダが空になり、応答がブラウザに捨てられていた

`_shared/util.ts` の `corsHeaders()` は、許可リストに一致しないと
**登録済みの最初のオリジン**を、リストが空なら**空文字**を返していた。

```
const allow = list.includes(origin) ? origin : (list[0] ?? "");
```

`Access-Control-Allow-Origin` は**オリジンと完全一致**でなければブラウザが応答を破棄する。
つまり関数自体は正常に動いていても、画面には結果が届かない。
しかも `fetch` は `TypeError: Failed to fetch` を投げるだけなので、
利用者にも開発者にも原因が分からない。決済・問い合わせが揃って失敗していたのはこのため。

現実に起きやすい設定を7通り並べて、修正前後で実際に実行して確かめた。

| 設定 | 修正前 | 修正後 |
|---|---|---|
| `ALLOWED_ORIGINS` 未設定 | `""` → 失敗 | 呼び出し元を許可 |
| `https://kinomin.github.io` | 一致 | 一致 |
| 末尾スラッシュ付き | `.../` → 失敗 | 一致 |
| `SITE_URL`と取り違え（パス付き） | パス付き → 失敗 | 一致 |
| 大文字混じり | 大文字のまま → 失敗 | 一致 |
| 複数指定の2番目に該当 | 1番目を返す → 失敗 | 一致 |
| 本当に別サイト | 拒否 | 拒否（維持） |

修正前は**7通り中5通りで失敗**する。`SITE_URL` にはパスが要るのに
`ALLOWED_ORIGINS` はオリジンだけ、という紛らわしさが直接の引き金になりやすい。
`new URL(...).origin` で正規化してから突き合わせ、未設定時は呼び出し元を許可するようにした
（認証はAuthorizationヘッダのJWTで行いCookieを使わないため、これで権限が緩むことはない）。
別サイトからの利用を拒否する動作は維持している。

### 併せて直したもの

1. **シークレット未設定で関数ごと起動しなくなる**
   `const stripe = new Stripe(requireEnv("STRIPE_SECRET_KEY"), ...)` がモジュール先頭にあり、
   未設定だと**モジュールの読み込み自体が失敗**する。`Deno.serve` に到達しないため
   CORSヘッダ付きの応答もプリフライトへの応答も返せず、ここでも「Failed to fetch」になる。
   `_shared/stripe.ts` に切り出して呼び出し時に生成するようにし、
   未設定でも理由の分かるJSONを返せるようにした。

2. **問い合わせが1回失敗すると60秒間なにをしても送れない**
   連続送信の記録（`touch_inquiry`）を**送信前**に消費していたため、転送に失敗すると
   再送も弾かれ、以降は「続けての送信はできません」しか出なくなる。
   失敗時は記録を戻すようにし、転送先の設定確認も記録より先に行うようにした。

3. **転送失敗の理由が握りつぶされていた**
   合言葉の不一致（`unauthorized`）もスクリプトプロパティの設定漏れも、
   画面には一律「送信できませんでした」としか出ず原因にたどり着けなかった。
   GAS側が返した理由を添えるようにした。

4. **支払ったのにプレミアムにならない経路があった**
   サインアップ時のトリガーが動かず `profiles` の行が無いと、`stripe_customer_id` を
   PATCHしても**0件更新が成功扱い**になって保存されない。その後のwebhookは顧客IDで
   会員を特定できず、支払いだけが済んだ状態になる。
   申し込み前に行の存在を保証（`ensureProfile()`）し、0件更新は例外にした。
   webhook側にも、顧客IDで見つからないときにmetadataの利用者IDで取り直す保険を入れた。

5. **有効期限がStripeのAPIバージョン変更で失われる**
   `current_period_end` は 2025-03-31(basil) 以降、購読アイテム側へ移動している。
   現在はSDKでバージョンを固定しているため影響しないが、更新時に静かに `null` に
   なるため、どちらの位置からも読めるようにした。

6. **画面側の見せ方**
   `Failed to fetch` をそのまま出さず、確認先（デプロイ状況・`ALLOWED_ORIGINS`）を
   添えるようにした。問い合わせの失敗はトーストではなくダイアログ内に残す
   （2.2秒で消えるうえ、書いた内容を失うため）。
   あわせて `dlgUpgrade()` 内の `const go` が画面遷移用の `go()` を隠していたので改名した
   （現時点では未使用のため不具合には至っていない）。

### 検証できたこと・できていないこと

CORSの判定は修正前後を実際に実行して7ケースで確認した。TypeScriptの構文と、
各関数のimport／使用シンボルの対応も機械的に確認している。
一方、**Supabase・Stripe・GASとの実通信はこの環境からsupabase.coへ接続できないため未検証**。
上記1〜5はコードの精読による判断で、実サービスでの確認は `DEPLOY.md` の
「決済・問い合わせが動かないときの確認順」に沿って行う必要がある。

---

## 18. アプリ名を「席メイト」に変更

「自動座席表作成ツール」から「席メイト」へ改名した。画面表示（ログイン画面・ヘッダー・タブタイトル）、
`README.md`、`terms.html`・`privacy.html`、問い合わせメールの件名・送信者名・本文（Google Apps Script）を
すべて置き換えた。過去の実装記録（第1〜17節）は当時の名称のまま残している。

機能・データ構造・APIの呼び出し方には変更がないため、動作確認は構文チェックのみで足りると判断した。

---

## 19. ディープレビュー（第4回）

決済・問い合わせの設定作業と並行して、アプリ本体をもう一度通して精読した。
今回は「実際に起きうる経路をたどって再現できたもの」だけを直している。

### 1.【重大】同期で座席表が消えると、編集画面がその場で落ちる

`renderEditorBody()` は先頭で `const k=ctx(); const{t,c}=k;` としており、
`ctx()` が `null` を返す場合を見ていなかった。同関数は座席のクリック・
ドラッグ・モード切替など15か所以上から直接呼ばれるため、ここで例外が出ると
編集画面が固まって何も操作できなくなる。

到達経路を実際に再現した。他の端末でテンプレートを削除すると、その削除記録が
同期で届き `mergeTemplates()` の結果からテンプレートが除かれる
（`merged` 0件を確認）。`store.data.templates` が置き換わった時点で
`ctx()` は `null` を返し、次の操作で
`TypeError: Cannot destructure property 't' of 'k' as it is null` が発生した。

`renderEditorBody()` は座席表が見つからないときに履歴（またはホーム）へ戻すようにし、
あわせて `snapNow()` / `applySnap()` / `doUndo()` / `doRedo()` と
教室備品の削除・移動にも同じ取りこぼしがあったため、まとめてガードを入れた。

### 2.【重大】同期でテンプレートが差し替わると、古い内容で上書き保存される

同期の取り込みは `store.data.templates` を丸ごと入れ替えるが、
編集画面のツールバーなどは**古いオブジェクトを掴んだまま**だった。
リモート側が新しくて差し替わったあとに「保存」を押すと、
古い内容が `updatedAt` を更新して書き戻され、他の端末での編集が消える。
取り込みの前後で、開いているテンプレートの実体が変わっていたら
画面を作り直すようにした。

### 3.【個人情報】共用パソコンで、旧データが別の先生に渡っていた

第13節で「アカウント間でデータが共有されていた」問題を直したが、
`ownerId` を持たない**旧データ**の引き取りが
「いまログインしている人のもの」という実装のままだった。
このため職員室の共用パソコンで、
先生Aの旧データが、あとからログインした先生Bのものになり、
**先生Bに先生Aの生徒名簿が見え、かつ先生Aは自分のデータを失う**。
第13節で塞いだはずの経路が、移行処理側に残っていたことになる。

引き取れるのはその端末で最初にログインした1人だけに限定した
（`claimLegacyOwner()`）。localStorageが使えない環境では引き取らない（安全側）。

### 4. ダイアログ表示中やCtrl+Sで交換モードが切り替わる

`S` の判定が `!inField` だけだったため、
ダイアログを開いている最中でも、また **Ctrl+S / Cmd+S（ブラウザの保存）** でも
交換モードが切り替わり、ダイアログの裏で編集画面が作り直されていた。
修飾キーとダイアログの表示状態を条件に加えた。

### 検証

修正後の分岐を実際に実行して10ケース確認（座席表が消えても落ちないこと、
戻り先が正しいこと、旧データの引き取りが最初の1人に限られること、
`S`の発火条件5通り）。あわせて座席配置アルゴリズムの回帰を7ケース確認した
（全員配置・重複なし・固定席の遵守・NGペアの非隣接・男女交互・前回と同じ席の回避・
席不足時に部分配置を採用しないこと）。

なお、Supabase・Stripe・GASとの実通信は引き続きこの環境から検証できていない。

---

## 20. ディープレビュー（第5回・名簿まわり）

決済と問い合わせが通るようになったため、今度はまだ精読していなかった名簿の
取り込み・編集まわりを重点的に見た。

### 1.【重大】転校生の入れ替えで出席番号が重複し、生徒が1名消える

出席番号は、座席への配置（`c.assign`）から生徒を引くための鍵になっている。
`roster.find(x => x.no === no)` は先頭の1件しか返さないため、番号が重複すると
**もう一方の生徒は座席に名前が出ず、ランダム配置でも席に着けない**。
しかも出席番号は画面から編集できないため、利用者は自力で直せない。

「1名ずつ追加」が `no: t.roster.length + 1` としていたのが原因。
学校で日常的に起きる操作でそのまま再現した。

```
初期:        1:田中 / 2:鈴木 / 3:佐藤
2番を削除:   1:田中 / 3:佐藤            ← 転校など
1名追加:     1:田中 / 3:佐藤 / 3:新入生  ← 3番が重複
```

この名簿で3席にランダム配置すると `{s_0_0:1, s_0_1:3, s_0_2:3}` となり、
3席が埋まっているのに実際には2名しか座れていない状態を確認した。

件数ではなく既存の最大値+1を使うよう修正（`nextRosterNo()`）。あわせて、

- **取り込み時**：CSV/xlsx側に重複があれば取り込む前に知らせ、
  「並び順のまま1から振り直して読み込む」か中止かを選べるようにした
- **既存データ**：すでに重複してしまっている名簿のために、名簿画面へ警告と
  「出席番号を振り直す」ボタンを出した（番号が変わるため、配置・固定席・
  NGペアが解除されることは事前に明示する）

### 2. 名簿が空でも固定席・NGペアを登録できた

生徒の選択肢が無いと `<select>` の値が空文字になり、`+""` が 0 になるため、
存在しない「0番」の固定席が作れてしまっていた。名簿の件数を確認するようにした。

### 検証

重複まわりを9ケース確認（途中削除→追加で重複しないこと、番号が飛んでいても
ぶつからないこと、重複の列挙、振り直しで並び順と氏名が変わらないこと、
振り直した名簿なら全員が別々の席に着けること）。
あわせて座席配置アルゴリズム7ケースと第19節の修正10ケースの回帰も確認した。

---

## 21. ディープレビュー（第6回・実際に画面を触っての確認）

これまではコードの精読が中心だったため、今回は**ブラウザで実際に操作して**
見つかったものだけを直した。Playwright + Chromium で
「テンプレート作成 → 名簿35名を取り込み → 座席レイアウト → 固定席 →
座席表を作成 → ランダム配置 → 印刷プレビュー」を通しで実行し、
パソコン幅(1280px)とスマートフォン幅(375px)の両方を確認している。
通しの操作でJavaScriptのエラーは出なかった。

### 1. 名簿を読み込んでも人数が「0名（男 0 / 女 0）」のまま

`paneRoster()` が人数を**描画時の文字列として埋め込んで**いたため、
その後 `draw()` で表を書き直しても見出しの人数が変わらなかった。

| 操作 | 表の行数 | 人数の表示（修正前） |
|---|---|---|
| 読み込み前 | 0 | 0名（男 0 / 女 0） |
| CSV 35名を読み込み | 35 | **0名（男 0 / 女 0）** |
| 1名追加 | 36 | **0名（男 0 / 女 0）** |
| 1名削除 | 35 | **0名（男 0 / 女 0）** |
| タブを開き直す | 35 | 35名（男 18 / 女 17） |

「読み込めたのか分からない」という、いちばん不安になる場面で数字が動かない。
人数の要素を保持し、`draw()` のたびに書き直すようにした。

### 2. 印刷プレビューで用紙の下端が枠からはみ出す

縮小率を `previewBox.clientHeight` から求めていたが、この値には
`padding:18px` が含まれる。差し引かずに使っていたため、
**用紙の下端が18.6px はみ出し**、最後列はスクロールしないと見えなかった。
「印刷前に全体を確認する」ためのプレビューなのに、その目的を果たせていない。

padding を差し引いて計算し、あわせて `transform:scale()` が配置上の大きさを
変えないことによる下側の余白も打ち消した（修正後は 17.4px の余裕をもって収まる）。
スマートフォン幅でも収まることを確認済み。

### 3. スマートフォンでアプリ名が「席メ／イト」と2行に割れる

編集画面はパンくず（ホーム › 2年3組 › 2026年8月1日〜）が長く、
上部バーの幅が足りなくなる。`.crumb` 側は横スクロールするようにしてあったが、
`.brand` に縮み止めが無かったため、アプリ名のほうが折り返していた
（実測：高さ26px → 46px）。`.brand` を `flex:0 0 auto; white-space:nowrap` にした。

### 確認できたこと

- スマートフォン幅でページ全体の横スクロールは発生しない（ページ幅=画面幅=375px）。
  はみ出すのは横スクロールさせる設計のツールバー内のボタンのみ。
- 編集画面で座席が用紙からはみ出していないこと（下 -23.7px / 右 -185.6px の余裕）。

回帰として、座席配置アルゴリズム7ケース・第19節分10ケース・第20節分9ケースを再実行し
すべて通ることを確認した。

---

## 22. β版のあいだ、課金の受付を止める

一般公開に先立ち、β版のあいだは有料会員の受付を止めて全員に無料で使ってもらう。
あとで課金を開始するときに元へ戻せるよう、**設定1か所の切り替え**で済ませている。

`APP_CONFIG.betaFreeMode`（既定 `true`）を `false` に戻すだけで課金を再開できる。
決済のしくみ（Stripe・Edge Functions・webhook）はコードもデータベースもそのまま残してある。

### `betaFreeMode: true` のあいだの動き

| 箇所 | 動き |
|---|---|
| ログイン画面 | 会員種別の2枚のカードを出さず、「β版につき、すべての機能を無料で」の案内に差し替え |
| ログイン直後 | プレミアム希望で入っても決済案内を出さない |
| ヘッダー | 「プレミアム」バッジを出さない |
| 設定メニュー | 会員種別の表示を「β版（無料）」に。「端末間の引き継ぎを使う」を出さない |
| アカウント設定 | 会員種別欄をβ版の案内に差し替え、「プレミアム会員に変更する」を出さない |
| 申し込み画面 | `dlgUpgrade()` は何も表示せず戻る（どこから呼ばれても開かない） |
| 印刷 | 回数を数えない。**サーバー側の残数も消費しない**（`consume_print` を呼ばない） |
| 残り回数の表示 | 印刷プレビュー内の案内を出さない |

### 規約・READMEとの食い違いを解消

利用規約に「無料会員の印刷は3回まで」と書いたままだと、実際の提供内容と 食い違う。
第3条の冒頭に「β版のあいだは有料会員の受付を行っておらず、印刷回数の制限もない」ことと、
「β版のあいだに料金が発生することはない」ことを明記した。READMEにも同じ注記を入れている。

### 検証

ブラウザで実際に確認した。

- ログイン画面：会員種別カード 0枚／「プレミアム」の文字 0件
- ヘッダー：プレミアムバッジ 0件
- 設定メニュー：`テスト先生 | β版（無料） | この端末のみに保存 | アカウント設定 | 問い合わせ | ログアウト`
- アカウント設定：「プレミアム会員に変更する」 0件
- 印刷：`printCount=99`（本来なら上限超過）の無料会員で**5回連続で印刷でき**、
  上限ダイアログは出ず、印刷回数のサーバー呼び出しも1件も発生しないことを確認
- `betaFreeMode:false` に戻したコピーで、会員種別カード2枚・「端末間の引き継ぎを使う」・
  「プレミアム会員に変更する」がすべて復活することを確認

通しの操作でJavaScriptのエラーは出ていない。既存の回帰（配置7・第19節10・第20節9）も再実行して通過。

---

## 23. β版のお試し用サンプルと、ランダム配置の取りこぼし修正

### 1. お試し用のサンプルを1つ用意する（β版のみ）

白紙から始める方針（第16節）は変えていないが、β版では
「名簿を用意しないと何も試せない」状態だったため、初回ログイン時に
見本のテンプレートを**1つだけ**作るようにした。

- クラス名「サンプル 1年1組」／担任「見本 太郎」／35名（男18・女17）／6×6の36席
- 出席番号順に並べた座席表を1枚つけてあり、開いてすぐ印刷まで試せる
- 実在の生徒と取り違えないよう、印刷物の見出しにも「サンプル」が出る

過去の不具合（第13節「全テンプレートを削除しても見本データが復活していた」）を
繰り返さないよう、**アカウントごとに一度だけ**作る。ブラウザで確認した動作：

| 場面 | テンプレート数 |
|---|---|
| 初回ログイン | 1（サンプルが入る） |
| 再読み込み | 1（増えない） |
| 削除して再読み込み | 0（**復活しない**） |
| 同じパソコンで別の先生がログイン | 1（その先生にも1つ用意される） |

すでに自分のテンプレートがある場合は作らない。`betaFreeMode` が false のときは作らない。

### 2.【重大】ランダム配置が、解があるのに4割方失敗していた

サンプルで「男女交互」を試したところエラーになった。調べると、
**35名（男18・女17）を6×6に配置という、ごく普通のクラスで再現**する。

30回試した内訳は 成功21・**探索の打ち切り9**・解なし判定0。
つまり解は存在するのに、探索の予算（40万ノード）を使い切って諦めていた。

原因は、同じ性別の生徒を**別の値として総当たりしていた**こと。
男女交互の判定に効くのは性別だけなので、男子18人の並べ替えはすべて同じ形になるが、
実装はその18通りを別々に試していた。行き詰まってから性別の並びを変えるには
膨大な入れ替えを戻す必要があり、そこで予算が尽きていた。

2つの対策を入れた。

- **同じ形をまとめる**：同性でNGペアも持たない生徒どうしは入れ替えても
  成立/不成立が変わらないため、1人だけ試す。
  （「前回と同じ席を禁止」は生徒ごとに禁止席が違い入れ替えられないので、その条件のときはまとめない）
- **打ち切ったら並べ直してやり直す**：打ち切りは並び順の運に左右されるため、
  5万ノード×最大8回に分けた（合計の予算は従来と同じ40万）。
  解が出た場合と「解なし」と確定した場合はその時点で終える。

40回ずつの計測結果：

| 条件 | 修正前 | 修正後 |
|---|---|---|
| 男18/女17・男女交互 | 30/40（1933ms） | **40/40**（34ms） |
| 男18/女17・男女交互＋前回と同じ席禁止 | 26/40（2866ms） | **40/40**（2184ms） |
| 男18/女18・男女交互 | 40/40 | 40/40 |
| 男20/女16・男女交互（解なし） | 0/40（正しく失敗） | 0/10（正しく失敗・23ms） |

解が存在しない条件で正しく失敗することは維持しており、
「部分的な配置を採用しない」方針も変えていない。
NGペア4組・固定席3件との併用、男女交互・前回と同じ席の違反0件、
20回試して20通りの異なる配置になること（ランダム性）も確認した。

---

## 24. ディープレビュー（第7回・仕上げ）

第23節でランダム配置の中身に手を入れたため、その周辺と、
これまで機械的な検算をしていなかった領域をまとめて確認した。

### 1.【重大】固定席が「前回と同じ席を禁止」で弾かれ、配置全体が失敗する

優先順位は **①固定席 ＞ ④前回と同じ席を禁止** と決めてあるのに、
固定した席にも④を適用していた。このため、

> 視力配慮などで毎回同じ席に固定している生徒がいるクラスで、
> 「前回と同じ席を禁止」にチェックを入れると、**その瞬間に配置全体が失敗する**

という状態だった。10通りの構成で検算したところ「全条件同時」だけが
0/30 で即座に失敗し、これで発覚した。固定した席は④の対象外にした。

### 2. 失敗の理由が分からないエラー文

固定席が条件に反したとき、実際の原因にかかわらず
「固定席どうしが条件に違反しています（NGペアが隣接、または男女交互が成立しない）」
としか出ず、上記1の場合は**書かれていない条件が原因**という状態だった。
違反した条件名・生徒名・座席の位置を出すようにした。

```
固定した座席が「NGペア」の条件に違反しています
佐藤 大輝さん（前から1列目・1番）の固定内容を見直してください
「固定席・NGペア」タブから固定を解除するか、席を変更できます
```

### 3. ふりがな欄の列がずれていても気づけなかった

「ふりがな」の列に漢字や英字が入っていると、ローマ字が空のまま取り込まれ、
座席表には氏名だけが並ぶ。半数以上でローマ字を作れなかった場合に知らせるようにした。
ふりがな欄が空のとき（意図的に使わない運用）は出さない。

### 検証（この節で新たに機械確認したもの）

- **配置結果の検算を10通りの構成で実施**（1席のみ／1列のみ／縦1列／歯抜けの座席／
  全員固定／固定席＋NG＋男女交互／NGペア10組／空席多数／8×8の60名／全条件同時）。
  成功した配置について、生徒の重複・名簿外の生徒・削除済み座席への配置・配置数・
  固定席の遵守・NGペアの隣接・男女交互・前回と同じ席をすべて検算し、違反0件。
- **ふりがな→ローマ字を17ケース**（拗音・促音・長音・カタカナ・空白／
  数字・記号・英字・漢字が混じっても例外を出さないこと）。
- **CSVの解釈を7ケース**（CRLF・カンマ入りセル・二重引用符・セル内改行・末尾改行なし・空セル）。
- **実際のExcelファイル(.xlsx)**をopenpyxlで生成して読み込み、5名が正しく取り込まれること。
  2枚目のシートを無視すること。
- **教員側表示の180度回転**：1番の生徒が 1/1 → 6/6 へ移動し、
  備品が 黒板144→597・教卓193→543・出口587→154 と反転すること。
- **用紙4通り（A4/B5 × 縦/横）**で座席・備品が用紙からはみ出さないこと。
- **実運用規模（12クラス×10枚＝120座席表・406KB）**で
  読み込み13ms・描画33ms、検索・編集・配置とも問題なく動作すること。

既存の回帰（配置7・第19節10・第20節9・第23節8）とあわせて **44件すべて通過**。
ブラウザでの通し操作でもJavaScriptのエラーは出ていない。

### 積み残し（把握しているが今回は変えていない）

- Supabase・Stripe・Google Apps Scriptとの実通信は、この開発環境から
  接続できないため引き続き未検証（本番では疎通確認済み）。
- 出席番号は画面から編集できない（重複時は「振り直す」で対応。第20節）。
- 8×8で60名という法定学級定員を超える規模では、条件によっては
  数百msかかることがある（40名までの現実的な規模では問題なし）。

---

## 25. 座席数を増やしたときの重なり・はみ出しの検証

「席数を増やしたときに入口・出口と重なったり、席が用紙からはみ出したりしないか」を
総当たりで確認した。

### 検証内容

座席数9通り（6×6／8×7／10×8／12×10／12×12／3×3／1×1／12×1／1×12）×
用紙2種（A4・B5）× 向き2種（縦・横）× 表示側2種（生徒側・教員側）の
**72通り**で、次を測定した。

- 座席が用紙の上下左右からはみ出していないか
- 教室備品（黒板・教卓・入口・出口）が用紙からはみ出していないか
- 備品と座席が1px²でも重なっていないか

結果は **72/72 で はみ出し・重なりなし**。
座席は実サイズを持っており（12×12でも画面上39.9×29.8px）、測定は有効に働いている。

さらに次も確認した。

- **利用者が備品を座席の真ん中へ動かした状態**で席数を6×6→8×8→12×12と増やす
  （生徒側・教員側の6通り）→ すべて自動で座席の外へ退避し、重なりなし
- **印刷時のサイズ計算（`FIT.print=true`）**でも同じか
  （6×6／10×8／12×12 × 縦横の6通り）→ すべて重なり・はみ出しなし

`fitSheet()` が座席を「部屋の高さの80%」に収まるよう毎回計算し直し、
`avoidRoomOverlaps()` が備品を最寄りの座席外へ逃がすため、
座席数を変えても破綻しない構造になっていることを確認できた。

### 見つかった問題：「＋前」「＋後」に行数の上限が無かった

はみ出し・重なりは起きないものの、**「＋後」を12回押すと18行になり、
際限なく増やせる**状態だった。「再生成」の入力欄は12までに制限されているのに、
ボタン経由だけ制限が抜けていた。

18行にすると座席は画面上26.5×19.8pxまで縮み、氏名が読めない座席表になる。
用紙からはみ出さないぶん、かえって「なぜか字が小さい」という分かりにくい壊れ方をする。

上限を `GRID_MAX=12` として一箇所に定義し、「再生成」と「＋前」「＋後」の
両方で同じ値を使うようにした。上限に達したときは
「行数は12行までです。これ以上増やすと座席が小さくなりすぎます」と知らせる。

---

## 26. 印刷時の氏名を大きく／ランダム配置で「今の配置を活かす」選択肢を追加

利用者から2点の要望があった。

- 印刷したときの氏名（漢字）が小さくて読みにくいので大きくしてほしい
- 一部の生徒だけ手動で席を決めて、残りをランダムに配置したいときがある。
  その場合に「今の配置を活かすか、全員決め直すか」を毎回選べるようにしてほしい

### 印刷時の氏名を大きく

`.seat .sname` はもともと1行固定（`white-space:nowrap`）で、画面編集用の
基準サイズをそのまま印刷にも使っていた。単純に印刷時の基準値だけ上げても、
1行に収まる幅で頭打ちになり見た目は変わらなかったため、`fitSheet()`の
氏名フォント計算（`--fs-name`）を印刷時だけ引き上げた。

- 印刷時の基準値・上限を、画面編集時より大きくする
  （上限は席の幅だけでなく高さにも連動させ、席が大きいほど氏名も大きくできるようにした）
- 折り返しは行わない（1行のまま）。長い氏名は`fitSeatNames()`が実際の
  席の幅に収まるぎりぎりの大きさまで自動で縮小する。短い氏名ほど大きく、
  長い氏名は必要な分だけ縮む
- 番号バッジと実際に重なっていないかをDOM上で直接確認し、
  重なっていれば解消するまで追加で縮小する保険を入れた
  （通常は1行のみなので起きないが、席が極端に小さい場合の保険として残した）

一度は2行までの折り返しを許可する案で実装したが、「折り返さず、できるだけ
大きく」という要望のため1行のままにする方針に変更した。

結果、4×4・A4横で「高橋蓮」「山本湊」のような3文字の氏名は座席いっぱいまで
大きく表示され、「田中結衣」のような4文字は席の幅に収まる分だけ縮む。
3×3・A4縦では52.5pxまで大きくなるなど、席数に応じて無理のない範囲で
氏名が大きく表示されるようになった。

### ランダム配置：「今の配置を活かす」か「全員決め直す」かを選べるように

「固定席・NGペア」で正式に固定していない、単なる手動配置（ドラッグ・交換モードで
置いた分）がある状態でランダム配置を開くと、新しく次の選択肢を出すようにした。

- **この配置を活かして、残りだけランダムに決める**（既定）
- **全員をランダムに配置し直す**

手動配置が無い場合はこれまでどおり選択肢を出さず、全員をランダムに配置する。

実装は `solve()` に `opt.keepSeats` を追加し、①固定席と同格に（ただし固定席を
優先し）事前に確定させたうえで、残りの生徒だけをNGペア・男女交互などの
条件を満たすよう配置する。手動で残した配置どうしが条件に違反している場合は
「残す指定をした配置が『○○』の条件に違反しています」という専用の文言で伝え、
固定席そのものの違反（従来の文言）と区別した。

単体テスト24件・実ブラウザでのUI確認（選択肢の表示条件、既定値、
「活かす」で特定の座席の生徒が維持されること、「決め直す」で実際に動くこと）を
確認済み。

### 印刷プレビューで見つかった2件の不具合（今回の変更とは無関係の既存不具合）

氏名を大きくする変更を実ブラウザでの印刷プレビュー経由で検証していたところ
（`FIT.print`を直接書き換えるのではなく、実際に「印刷 / PDF」ボタンを押して
確認する方法に切り替えたところ）、今回の変更とは関係のない既存の不具合を2件見つけた。
以前（第25節）の72/72通過という結果は、`FIT.print`を直接書き換えて確認する方法で
テストしており、この2件はその方法では再現しなかったため見逃していた。

**原因はどちらも同じで、`.seat{transition:.1s;width:var(--cw);height:var(--ch)}`という
画面編集中の滑らかな拡縮用のCSSが、印刷時にも働いてしまっていたこと。**
印刷用の`--cw`／`--ch`に切り替えた直後、`fitSeatNames()`や`avoidRoomOverlaps()`が
座席の重なり判定をする時点では、まだ0.1秒の移行アニメーションの途中（＝画面編集時の
古いサイズに近い状態）で座席サイズを読み取ってしまい、実際に印刷される最終サイズでの
判定になっていなかった。

- **教室備品（入口・出口など）と座席が重なる**：A4横の8×6などで発生
- **氏名と出席番号バッジが重なる**：B5横でほぼすべての席数で発生
  （B5は用紙が小さく座席1つあたりの余白が少ないため、影響が出やすかった）

印刷プレビュー・実際の印刷では`.seat`の`transition`を無効にし、`--cw`／`--ch`を
切り替えた瞬間に最終サイズへ即座に反映されるようにして解消した。あわせて、
印刷プレビューの縮小表示（`transform:scale()`）が反映されたあとにも
`avoidRoomOverlaps()`をもう一度呼び直し、備品の重なり判定が縮小後の実測値で
行われるようにした（transition無効化だけでも解消したが、二重の安全策として残した）。

修正後、実際の印刷プレビュー経由で列×行8通り×用紙/向き3通り＋長い氏名3通りの
**27/27で問題なし**を確認。既存の回帰テスト（配置・検算・履歴・氏名フォームなど）も
あわせて確認し、新たな崩れがないことを確認した。

修正後は「＋後」を12回押しても12行で頭打ちになり、座席は29.8×39.9pxを保つ。
